<!--
Copyright © 2026 姚金刚. All rights reserved.
Project: yao-geo-page-blueprint
Created by: 姚金刚
Date: 2026-05-16
X: https://x.com/yaojingang
-->

# 页面蓝图报告版式

- 借鉴 `kami` 的专业文档纪律：清晰层级、紧凑信息密度、表格节奏、留白克制、证据优先。
- 报告一律使用白底主画布；可使用极浅暖白卡片、暖灰边框和油墨蓝强调，但不要用深色底、渐变、纹理、装饰性块面或过重阴影。
- 中文标题使用 serif fallback，正文和功能标签使用 sans；行距控制在 1.45-1.55，避免松散网页感。
- 表格必须有边框、表头背景、稳定列宽、长文本换行和可读行距。
- HTML、PDF、Word、Markdown 必须共用同一内容结构和同一事实来源。
- Markdown 使用语义标题和标准表格。
- HTML 设置 `border-collapse`、`overflow-wrap`、响应式表格和 A4 打印。
- HTML 可视化报告必须有 `报告目录` 菜单栏，使用 `position: sticky; top: 0` 在页面下拉时固定跟随；菜单项用锚点跳转到每个模块，移动端允许横向滚动。
- PDF 从已校验 HTML 渲染，白底、A4、页边距稳定。
- Word 保留标题层级、表格边框和可复制文本，不交付整页图片。
- Word 表格必须使用固定表格网格、显式总宽度、显式列宽、单元格内边距和段落断词；存在长 URL、英文产品名、Schema 字段和代码片段时必须插入软换行，防止向右溢出。
- 报告内容必须系统、详细、完整：不能只有模块清单，要包含输入假设、研究依据、query fan-out、实体关系、证据台账、抽取单元、实施验收和待确认项。
